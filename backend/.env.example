# KV storage (recommended for prod)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Internal callback auth (required in production for /api/internal/*)
INTERNAL_API_KEY=dev-internal-key

# Worker convenience
BACKEND_BASE_URL=http://localhost:3000

# Queue consumer
WORKER_POLL_MS=1500

# Shopee base URL for keyword/shopId crawling
SHOPEE_BASE_URL=https://shopee.sg
